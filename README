This is a Haskell module for parsing Multi-Threaded Routing Toolkit (MRT) Routing
Information Export Format files, as described by RFC6396.
So far it only converts to a IPv4 IPRTable, but another function can be added
for other table types as the parser is there.

